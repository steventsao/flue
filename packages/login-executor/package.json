{
	"name": "@flue/login-executor",
	"version": "0.0.0",
	"type": "module",
	"private": true,
	"license": "Apache-2.0",
	"bin": {
		"flue-login-worker": "dist/cli.mjs",
		"flue-login-local": "dist/local-cli.mjs"
	},
	"exports": {
		".": {
			"types": "./dist/index.d.mts",
			"import": "./dist/index.mjs"
		}
	},
	"scripts": {
		"build": "tsdown",
		"check:types": "tsc --noEmit",
		"test": "vitest run"
	},
	"dependencies": {
		"@earendil-works/pi-ai": "0.80.2",
		"@flue/runtime": "workspace:*",
		"hono": "^4.8.3"
	},
	"devDependencies": {
		"tsdown": "^0.22.0",
		"typescript": "^6.0.3",
		"vitest": "^4.1.6"
	}
}
